{
  "name": "new-steammakers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n vite,proxy -c cyan,magenta \"vite --host 127.0.0.1 --port 3000\" \"node server.js\"",
    "server": "node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && node server.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "blockly": "^12.5.1",
    "express": "^5.2.1",
    "lucide": "^1.11.0"
  }
}
